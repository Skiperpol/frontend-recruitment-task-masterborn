# ---------------------------------------------
# Example environment configuration file (.env)
# ---------------------------------------------
# Copy this file and rename it to ".env" before running the project.
# Adjust the values according to your local or production setup.

# -------------------------------------------------------
# FRONTEND CONFIGURATION
# -------------------------------------------------------

# Base URL of the backend API used by the frontend (Vite).
# This should point to your backend server.
VITE_API_URL='http://localhost:3000'


# -------------------------------------------------------
# LOCAL TEST SETTINGS
# -------------------------------------------------------
# The following values are used for local testing and development only.
# Change or remove them when deploying to production.

# Port where the Vite development server runs (frontend).
VITE_PORT=5173

# Port where the backend API runs during local testing.
API_PORT=3000

# Hostname used for both frontend and backend during local testing.
HOST=localhost